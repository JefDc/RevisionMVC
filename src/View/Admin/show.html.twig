{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <h1>Utilisateur {{ user.name }}</h1>
        <p>son email: {{ user.email }}</p>
        <p>son password:  {{ user.password }}</p>
    </div>
{% endblock %}